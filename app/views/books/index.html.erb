<h1>Books</h1>

<% @books.each do |book| %>



  <p>投稿ユーザー画像:<%= image_tag book.user.get_profile_image(100,100) %></p>

  <p>Title</p>
  <span>
    <%= link_to book.title, book_path(book.id) %>
  </span>

  <p>Opinion</p>
  <span><%= book.body %></span>

<% end %>
